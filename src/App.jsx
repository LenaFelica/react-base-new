import { Layout } from '@/components/layout';
import { MainPage } from '@/pages/main-page';

import './styles/index.scss';

export const App = () => {
  return (
    <Layout>
      <MainPage />
    </Layout>
  );
};
